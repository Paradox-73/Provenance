Let me read all the files before giving you a full diagnosis.Good, now I have the full picture. Let me give you a complete diagnosis.

---

**Current Score: 2 found, 6-7 missed, 1 false positive**

Let me break down every issue by root cause.

---

## False Positives

**Identity Conflict (Aris/Evans) — Confidence 1.00 is wrong, but the detection itself isn't**

This is actually a real conflict, not a false positive. The source texts are just ugly — `"Dr. Aris was the only one..."` and `"But Dr. Evans..."` are from Chapter 3, not Chapter 1, and they come from different chapters than the actual conflict. The real issue is that your `HAS_IDENTITY` edges for the same scene (Chapter 1, sentence 27 vs 29) aren't being created at all — because `extract_triples` in spacy mode only fires `HAS_IDENTITY` when it sees the literal string `dr. evans` or `dr. aris`, and those strings appear in Chapter 1 too. Check what's actually in the graph: the identity conflict is being caught via Chapter 3 evidence instead of Chapter 1. The fix is ensuring your `provenance.source_text_snippet` points to the correct sentence. Not a false positive — just a wrong source citation.

The NLI confidence of 1.00 is suspicious though. Look at `detect_identity_inconsistency`: it runs `run_nli_check("Aris is officially identified as Aris.", "Aris is officially identified as Evans.")` — these are trivially contradictory to DeBERTa, so the high confidence is expected.

---

## Misses and Root Causes

### 1. Attribute Conflict (Kael's scar) — `DEBUG: No potential attribute conflicts found in graph`

The graph has zero `HAS_ATTRIBUTE` edges. Here's why: in spacy mode, `extract_triples` is the only triple extractor running (line 375). It correctly has scar/smooth detection in section C. But look at what it emits as the `source_text_snippet`:

```python
"provenance": {"source_text_snippet": sent_text}
```

Then in `neo4j_adapter.py` Pass 2, this filter runs first:

```python
if any(bad_phrase in source_text.lower() for bad_phrase in ["like sand", "report,"]):
    continue
```

That's not the problem here. The problem is the `MERGE` statement for `HAS_ATTRIBUTE`:

```python
"MERGE (s)-[r:HAS_ATTRIBUTE]->(o) "
"SET r.chapter = $chap, r.text = $txt"
```

`MERGE` on a relationship pattern without a uniqueness key means that if `KAEL -[HAS_ATTRIBUTE]-> jagged_scar` already exists, Neo4j won't create a second one for `smooth unblemished face` — it'll match the first edge and overwrite it. But actually the real problem is simpler: the `object` node for `jagged scar` and `smooth unblemished face` — what are their `canonical_ids`?

In `process_chapters_for_nlp`, canonical ID assignment for triple nodes does:
```python
if label in ['PERSON', 'TITLE']:
    triple[role]['canonical_id'] = resolved_text.split()[-1].upper()
else:
    triple[role]['canonical_id'] = resolved_text.upper().replace(' ', '_')
```

The label lookup only works if the entity appears in `chunk['entities']`. `jagged scar` and `smooth unblemished face` will never be in spaCy's NER output — they're not named entities. So `label` stays `"UNKNOWN"`, and `canonical_id` becomes `JAGGED_SCAR` and `SMOOTH_UNBLEMISHED_FACE`. That part is fine.

So go back to the actual question: **is `extract_triples` even being called in spacy mode?** Look at line 374:

```python
else:
    all_chunk_triples = extract_triples(resolved_content)
```

This is the `else` branch of `if mode in ["llm", "hybrid"]`. So in spacy mode, `extract_triples` runs. But `extract_triples` emits the full `sent_text` as `source_text_snippet`, and `sent_text` for the scar sentence is: `"The jagged scar running down his left cheek seemed to glow in the red emergency lighting of the Bridge."` — that doesn't match either bad phrase filter. So it should reach Neo4j.

**The actual culprit**: in `neo4j_adapter.py`, the relationship routing checks `pred_label`:
```python
elif pred_label in ["ATTRIBUTE", "HAS_ATTRIBUTE"]:
```

But also checks `pred_text` against the location pattern first:
```python
if re.search(location_pattern, pred_text) or pred_label == "LOCATED_AT":
```

Where `location_pattern = r"\b(at|in|on|puts you at|located)\b"`. The attribute triple has `pred_text = "has"` — that doesn't match. Good. But check your `MERGE` for attributes:

```python
"MERGE (s:Entity {canonical_id: $sid}) "
"MERGE (o:Entity {canonical_id: $oid}) "
"MERGE (s)-[r:HAS_ATTRIBUTE]->(o) "
"SET r.chapter = $chap, r.text = $txt"
```

**This is the bug.** `MERGE (s)-[r:HAS_ATTRIBUTE]->(o)` without a relationship key means Neo4j merges on the entire pattern — if there's already *any* `HAS_ATTRIBUTE` edge between `KAEL` and `JAGGED_SCAR`, it won't create a new one. But since each attribute object has a different `canonical_id`, this should work fine between different objects.

The real question is whether `extract_triples` is actually emitting these triples and whether they're surviving the ingestion. Add this debug line temporarily in `neo4j_adapter.py` right before the routing block:

```python
print(f"DEBUG TRIPLE: subj={subj_id}, pred_label={pred_label}, obj={obj_id}, src={source_text[:50]}")
```

My strong bet: `extract_triples` is not reaching the attribute sentences because of how `chunk_text` splits the document. The scar sentence and the smooth face sentence are likely in the same chunk, but look at this line in `document_parser.py`:

```python
current_length += sentence_length + 1
```

With `chunk_size=3000`, the entire Chapter 1 is likely one chunk. That's fine. But `resolved_content = content` (coreference is bypassed), so the spacy `doc` in `extract_triples` is processing the whole chunk at once. The scar detection regex is:

```python
if "scar" in sent_lower or "smooth" in sent_lower or "unblemished" in sent_lower:
```

And Chapter 1 sentence 15 contains `"scar"` and sentence 34 contains `"smooth"` and `"unblemished"`. So both should fire. Unless... the `extracted_this_sent` key collision is the problem:

```python
key = f"ATTR_{subj}_{attr_desc}"
```

For sentence 15, `attr_desc = "jagged scar"`, `subj = "Kael"` → key = `ATTR_Kael_jagged scar`. For sentence 34, `attr_desc = "smooth unblemished face"`, `subj = "Kael"` → key = `ATTR_Kael_smooth unblemished face`. These are different keys so no collision. Both should be emitted.

**Then the actual problem must be in ingestion.** Run with the debug print and check if these triples appear. If they do appear but the `ATTRIBUTE_CONFLICT_QUERY` returns nothing, the issue is that the `HAS_ATTRIBUTE` nodes exist but the query requires `r1.chapter = r2.chapter` — both scars are in Chapter 1, so that should match. Unless the chapter assignment is wrong.

**Fix**: Add the debug print, confirm the triples exist in Neo4j by running this directly in the Neo4j browser:
```cypher
MATCH (p:Entity)-[r:HAS_ATTRIBUTE]->(a:Entity) RETURN p.name, a.name, r.chapter
```

If nothing returns, the triples are being dropped at ingestion. The most likely cause is that `pred_label` is somehow not matching `"HAS_ATTRIBUTE"` — check by printing `pred_label` for attribute triples.

---

### 2. Chapter 2 Temporal Conflict (Maria at Captain's Quarters AND Hangar Bay at 10:00)

The timestamp normalization you added should fix the `10:00_HOURS` vs `10:00` mismatch. But look at what `extract_triples` actually emits for Chapter 2.

Sentence: `"At 10:00 hours, Maria entered the Captain's Quarters..."` — the time tracker fires and sets `current_time = "10:00 hours"`. Then the location check: `"captain's quarters"` is in the hardcoded list. `"enter"` matches the verb list. So a triple `MARIA -[LOCATED_AT]-> Captain's Quarters` at timestamp `10:00 hours` is emitted. ✓

Sentence about Hangar Bay: `"Maria was pacing the Hangar Bay..."` — `"hangar bay"` is in the list, `"pace"` matches. But what is `current_time` at this point? The time tracker only fires if there's a time mention in the current sentence. Between the Captain's Quarters sentence and the Hangar Bay sentence, is there a time mention? Looking at the text: no explicit time between them. So `current_time` stays as `"10:00 hours"` from the last update. After normalization, both should be `"10:00"`. So this conflict should now fire.

**But wait** — there's a subtlety in the Cypher query:
```cypher
AND NOT (l1)-[:PART_OF*1..2]-(l2)
```

Captain's Quarters and Hangar Bay have no `PART_OF` relationship between them, so this filter won't suppress it. The conflict should fire after your timestamp normalization. If it doesn't, verify the canonical IDs: `THE_CAPTAIN'S_QUARTERS` vs `THE_CAPTAIN_S_QUARTERS` — the apostrophe in the canonical ID could cause issues in Neo4j. Add this to your canonical ID normalization:

```python
canonical_id = resolved_text.upper().replace(' ', '_').replace("'", "")
```

Apply this everywhere canonical IDs are generated for locations.

---

### 3. Chapter 5 Inventory Conflict (Kael holds key AND lost it in reactor core)

The text says Kael holds the Key and then says `"I also lost the Red Onyx Key in the reactor core earlier this morning."` These are in the same chapter (Chapter 5). Your `INVENTORY_CONFLICT_QUERY` looks for `POSSESSES` and `LOST` in the same chapter. 

In `extract_triples`, the POSSESSES trigger requires: `any(v in sent_lower for v in ["have", "pull", "put", "hold", "log", "place"])`. The sentence `"She was facing Kael, or the entity that looked like him, and he was holding the Red Onyx Key"` — `"holding"` is not in the list. Add `"hold"` and `"holding"` explicitly. Currently only `"hold"` is listed, but Python's `in` check on `"hold"` against `"holding"` — wait, `"hold" in "holding"` is `True` in Python string containment. So it should match.

The LOST trigger: `"I also lost the Red Onyx Key in the reactor core earlier this morning."` — `"lost"` is in the trigger list. So the LOST triple should fire. 

The real problem: these two sentences might be in **different chunks** if Chapter 5 is long enough to exceed `chunk_size=3000`. And `current_time` is a local variable that resets per `extract_triples` call. If Kael's possession is in chunk 0 and the LOST statement is in chunk 1, `a1.chapter = a2.chapter` still matches (both are `"Chapter 5"`), but they're separate `Assertion` nodes, so the query should still find them.

However, look at the POSSESSES triple subject assignment:
```python
subj = "Kael" if any(w in sent_lower for w in ["kael", " i ", "he "]) else active_subj
```

For the LOST sentence, `"I also lost..."` — `" i "` with spaces won't match `"i also"` at sentence start. The sentence begins with a quote, so it's `"\"i also lost..."` — `" i "` needs a space before it, which won't exist at the start. **Fix**: change to `re.search(r'\bi\b', sent_lower)` instead of `" i "` string containment.

---

### 4. Chapter 6 Temporal Conflict (Maria on Bridge AND Engine Room at 12:00)

Same timestamp normalization issue as Chapter 2. After your fix, `"12:00 hours"` and `"that very same 12:00 hours"` should both normalize to `"12:00"`. The location list includes `"bridge"` and needs `"engine room"` — check your hardcoded list:

```python
locations = ["bridge", "engineering deck", "medical bay", "captain's quarters", "hangar bay", "tactical station", "hangar"]
```

**`"engine room"` is missing from the location list.** Add it. Also add `"reactor core"` for Chapter 5.

---

### 5. Chapter 6 Eye Color Attribute Conflict (Kael: blue eyes → green eyes)

Same root cause as the scar — `HAS_ATTRIBUTE` not reaching the graph. But additionally, `extract_triples` section C only watches for `"scar"`, `"smooth"`, `"unblemished"`. Eye color is completely unhandled. You need to extend section C:

```python
# Eye color detection
eye_color_match = re.search(r'(blue|green|brown|grey|gray|unnatural \w+)\s+eyes?', sent_lower)
if eye_color_match:
    subj = "Kael" if any(w in sent_lower for w in ["kael", "his", "him"]) else active_subj
    attr_desc = eye_color_match.group(0)
    key = f"ATTR_{subj}_{attr_desc}"
    if key not in extracted_this_sent:
        triples.append({...})
```

---

### 6. Chapter 4 Attribute Conflict (Red Onyx Key: small smooth stone → large jagged shard)

`extract_triples` doesn't handle object attributes at all — section C only handles person attributes (scar, smooth skin). The Key's physical description changes are never extracted. You need a new section D.5 (or extend C) for object attributes:

```python
# Object attribute detection
key_descriptors = re.search(r'(small.*?stone|large.*?shard|smooth.*?stone|jagged.*?shard)', sent_lower)
if key_descriptors and any(k in sent_lower for k in ["red onyx", "key", "stone", "shard"]):
    attr_desc = key_descriptors.group(0)[:50]
    key = f"ATTR_THE_RED_ONYX_KEY_{attr_desc}"
    if key not in extracted_this_sent:
        triples.append({
            "subject": {"text": "The Red Onyx Key"},
            "predicate": {"label": "HAS_ATTRIBUTE", "text": "is described as"},
            "object": {"text": attr_desc},
            "timestamp": current_time,
            "provenance": {"source_text_snippet": sent_text}
        })
```

Note that your `ATTRIBUTE_CONFLICT_QUERY` queries `(p:Entity)-[r:HAS_ATTRIBUTE]->` — `The Red Onyx Key` is stored as an Entity, so this should work.

---

## Summary of All Fixes

In priority order:

**`nlp_pipeline.py` → `extract_triples`**:
1. Add `"engine room"` and `"reactor core"` to the locations list
2. Fix the `" i "` pronoun match to use `re.search(r'\bi\b', sent_lower)` 
3. Extend attribute detection to handle eye color and object (key) descriptions

**`neo4j_adapter.py`**:
4. Strip apostrophes from canonical IDs: `.replace("'", "")`
5. Add the debug print for a single run to confirm `HAS_ATTRIBUTE` triples are actually reaching ingestion

**`graph_logic.py`**:
6. Once attributes are confirmed in the graph, the `similarity > 0.2` threshold with `confidence > 0.7` NLI check should work — but test it because MiniLM similarity between `"jagged scar"` and `"smooth unblemished face"` might still come in under 0.2 given they share no vocabulary. If so, for physical appearance attributes specifically, skip the semantic gatekeeper and go straight to NLI.